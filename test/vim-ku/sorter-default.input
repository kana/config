" Test: vim-ku sorter-default
function s:describe__ku_sorter_default_sort()  "{{{1
  let args = {}
  let candidates = [
  \     {'word': 'j'},
  \     {'word': 'u'},
  \     {'word': 'n'},
  \     {'word': 'i'},
  \   ]

  It should not sort if default sorters is []

  let default_sorters = ku#sorter#default#use([])
  Should ku#sorter#default#sort(copy(candidates), args)
  \ ==# [{'word': 'j'}, {'word': 'u'}, {'word': 'n'}, {'word': 'i'}]

  It should sort candidates by dictionaryr order with the default sorter

  call ku#sorter#default#use(default_sorters)
  Should ku#sorter#default#sort(copy(candidates), args)
  \ ==# [{'word': 'i'}, {'word': 'j'}, {'word': 'n'}, {'word': 'u'}]
endfunction




function s:describe__ku_sorter_default_use()  "{{{1
  It should return the defult sorters at first

  let default_sorters = ku#sorter#default#use([])
  Should default_sorters ==# [function('ku#sorter#simple#sort')]

  It should return the previously used sorters

  let new_sorters = ku#sorter#default#use(default_sorters)
  Should new_sorters ==# []
endfunction




" __END__  "{{{1
" vim: filetype=vim foldmethod=marker
